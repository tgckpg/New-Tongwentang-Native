language: cpp
sudo: required
dist: trusty
compiler:
- gcc
os:
- linux
before_install:
- echo $LANG
- echo $LC_ALL
- sudo apt-get install -y php5
branches:
  only:
  - master
  - "/^v\\d+\\./"
notifications:
  email: false
env:
  global:
  - LANG="en_US.UTF-8"
